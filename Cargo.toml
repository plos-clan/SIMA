[workspace]
members = [
    "sima-init",
    "sima-cli",
    "sima-proto",
    "xtask",
]
default-members = ["sima-init", "sima-cli", "sima-proto"]
resolver = "3"

[profile.release]
lto = false
opt-level = "z"
